export class City {
    constructor({ code, name, state, lat, lng }) {
        this.code = code;
        this.name = name;
        this.state = state;
        this.lat = lat;
        this.lng = lng;
    }
}
